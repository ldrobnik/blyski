# Redirect default Netlify subdomain to primary domain
https://blyski.netlify.app/* https://blyski.netlify.app/:splat 301!

# Prevent errors when accessing routes other than '/'
/*    /index.html   200