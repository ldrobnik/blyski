import React, {useState, useEffect} from 'react';
import styled from 'styled-components';


const Text = (props) => {

    return (
        <React.Fragment>
            Text
        </React.Fragment>
    );
};

export default Text;